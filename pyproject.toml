[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pastedb"
version = "1.0.3"
description = "PasteDB Python SDK"
authors = [{ name="Aditya" }]
dependencies = ["requests"]
